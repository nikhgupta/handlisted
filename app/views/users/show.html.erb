<%= render "profile_box", presenter: present(@user), liked: @liked, found: @found %>

<div class="row">
  <div class="col-md-12 text-center">
    <ul class="nav nav-pills mb20">
      <li class="active bg-alert">
        <a href="#found-products" data-toggle="tab" class="btn">Products He <%= fa_icon('search') %></a>
      </li>
      <li class="bg-danger">
        <a href="#liked-products" data-toggle="tab" class="btn">Products He <%= fa_icon('heart') %></a>
      </li>
    </ul>
  </div>
</div>

<div class="tab-content br-n pn">
  <div id="found-products" class="tab-pane active">
    <%= products_listing_for @products[:found], kind: :found do %>
      Oh snap! <strong>@<%= @user.username %></strong> seems a bit shy. He needs to
      <a href="#" class="alert-link">find some products</a>
      for you to be able to discover his taste!
    <% end %>
  </div>
  <div id="liked-products" class="tab-pane">
    <%= products_listing_for @products[:liked], kind: :liked do %>
      Oh snap! <strong>@<%= @user.username %></strong> seems a bit shy. He needs to
      <a href="#" class="alert-link">like some products</a>
      for you to be able to discover his likes!
    <% end %>
  </div>
</div>
