<div class="panel heading-border panel-success">
  <div class="panel-body bg-light">
    <%= form_for(@user, url: edit_profile_path, html: { class: "form-horizontal", role: "form" }, builder: UserFormBuilder) do |f| %>
      <% if @user.errors.any? %>
        <div class="alert alert-danger alert-dismissable" role="alert">
          <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
          <h4><%= pluralize(@user.errors.count, "error") %> encountered:</h4>
          <ul>
            <% @user.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="row">
        <div class="col-md-6">
          <%= f.text_field :name, autofocus: true, placeholder: "Enter email", class: "gui-input", icon: 'user' %>
        </div>
        <div class="col-md-6">
          <%= f.email_field :email, placeholder: "Enter email", class: "gui-input", icon: 'user' %>
        </div>
      </div>
      <div class="row">
        <div class="col-md-6">
          <%= f.password_field :password, autocomplete: 'off', class: "gui-input", icon: 'lock' %>
        </div>
        <div class="col-md-6">
          <%= f.password_field :password_confirmation, autocomplete: 'off', class: "gui-input", icon: 'lock' %>
        </div>
      </div>
      <%= f.submit 'Update info', class: "btn-success" %>
    </div>
  <% end %>
</div>
