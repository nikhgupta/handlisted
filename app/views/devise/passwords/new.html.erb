<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, builder: UserFormBuilder) do |f| %>
  <div class="panel-body bg-light p30">
    <div class="row">
      <div class="col-sm-7 pr30">
        <%= devise_error_messages! %>

        <%= f.text_field :login, autofocus: true, placeholder: "Email or Username", class: "gui-input", icon: "user", required: true, minlength: 4 %>
      </div>
      <div class="col-sm-5 devise-aside">
        <%= link_to "Log in", new_user_session_path, class: "btn btn-sm btn-primary" %>
        or
        <%= link_to "Register", new_user_registration_path, class: "btn btn-sm btn-primary" %>
      </div>
    </div>
  </div>
  <div class="panel-footer clearfix p10 ph15">
    <%= f.submit 'Reset my Password', class: "pull-left" %>
  </div>
<% end %>
