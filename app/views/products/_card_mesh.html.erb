<%# FIXME: belongs to a decorated collection? %>
<% products.each do |product| %>
  <div class="col-xs-4 col-sm-2 col-md-1 product-mini-card">
    <%= product.decorate.cover_image_tag height: 70 %>
  </div>
<% end %>
