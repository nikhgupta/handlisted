#= require jquery
#= require jquery_ujs
#= require_self
#= require theme
#= require users
#= require products
#= require services

"use strict"

$(document).ajaxError (e, XHR, options) ->
  # TODO: display a message here saying that the action requires login
  message = "You need to sign in or sign up before continuing."
  window.location.replace("/login?error=#{message}") if XHR.status == 401
